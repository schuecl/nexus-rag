# Direct runtime dependency of seed_sample_data.py (scripts/Dockerfile).
# Compiled to requirements.txt with `pip-compile --generate-hashes`; see
# issue #230.
httpx>=0.28.1
# Issue #309: calibrate_tagging_advisory.py connects directly to Postgres as
# the dedicated, SELECT-only audit-reporting role (see that script's module
# docstring for why it doesn't go through any service's HTTP API instead).
# psycopg3, same driver family as `postgresql+psycopg://` elsewhere in this
# repo (services/common/common/db.py); [binary] avoids needing libpq-dev in
# the scripts image (scripts/Dockerfile is python:3.11-slim, not one of the
# service images that already have build tooling).
psycopg[binary]>=3.1
