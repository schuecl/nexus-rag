[mypy]
# Enforced scope: services/common (see .github/workflows/ci.yml and
# docs/testing.md). Third-party libs used there (qdrant-client, fastembed,
# nats-py, boto3) ship no type stubs, so missing imports are ignored rather
# than blocking the gate.
ignore_missing_imports = True
explicit_package_bases = True
