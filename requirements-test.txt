# Pinned test/lint toolchain for CI (.github/workflows/ci.yml) and local runs
# (docs/testing.md). Install alongside each service's own dependencies.
# NOTE: pytest is held below 9.1, not below 9 -- pytest-bdd 8.1.0 uses fixture
# registration that 9.1 removed, but 9.0.x is fine with it (verified by running
# tests/e2e's BDD scenarios against this pin). 9.0.3 is also where
# PYSEC-2026-1845 (predictable /tmp/pytest-of-{user}) is fixed, so this pin
# closes that alert rather than accepting it. Re-check the upper bound when
# pytest-bdd ships a 9.1-compatible release (still 8.1.0 as of 2026-08-06;
# dependabot's 9.1.1 bump failed every BDD scenario on exactly this, twice --
# most recently PR #481, 12 failures on both 3.11 and 3.12). That bump kept
# being re-proposed because .github/dependabot.yml only ignored pytest MAJORS
# and 9.0 -> 9.1 is a minor; it now ignores minors too, so raising this pin
# means dropping that ignore entry in the same change.
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-cov==7.1.0
pytest-bdd==8.1.0
# #111: scripts/check_compose_hardening.py parses docker-compose.yml, and
# tests/unit/test_check_compose_hardening.py imports it.
pyyaml==6.0.3
respx==0.23.1
httpx==0.28.1
beautifulsoup4==4.15.0
# ingestion-worker parser dependencies exercised by tests/unit/ingestion_worker
pypdf==6.14.2
python-docx==1.2.0
python-pptx==1.0.2
openpyxl==3.1.5
# #92: image decode/size-filter in ingestion-worker's captioning tests (the
# tests also use it to author fixture images/PDFs in memory)
pillow==12.3.0
# #241: OCR wrapper; unit tests mock image_to_string, no tesseract binary
# needed outside the worker container.
pytesseract==0.3.13
# #134: common/tracing.py's OpenTelemetry stack (unit tests build a real
# in-memory TracerProvider to assert span trees and NATS header propagation)
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0
# #160: the Milvus backend's client (unit tests cover the FR-26 expression
# builder and backend selection; live parity runs against a real container)
pymilvus==3.0.1
# #349: common/profiling.py's Pyroscope SDK (unit tests mock pyroscope.configure,
# but importing the module -- which every test collection does transitively --
# still needs the package installed)
pyroscope-io==1.2.1
# #444/#445: common/security_headers.py's ASGI middleware is built on
# starlette's ASGI type aliases; tests/unit/common/test_security_headers.py
# drives it through a real Starlette app + TestClient. Pinned to the same
# version every service's own lockfile resolves transitively (via FastAPI or
# MCPServer's Starlette app), so this test env matches the real one.
starlette==1.3.1
