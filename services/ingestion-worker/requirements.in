# Direct runtime dependencies of ingestion-worker (see pyproject.toml), plus
# services/common's own deps (-r) since this image installs common from
# local source with --no-deps and needs common's deps satisfied from this
# lockfile instead. nexus-rag-common itself is installed from local source,
# not from this file. Compiled to requirements.txt with
# `pip-compile --generate-hashes`; see issue #230.
-r ../common/requirements.in
opentelemetry-instrumentation-httpx>=0.51b0
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
qdrant-client>=1.10
prometheus-client>=0.20
pypdf>=6.14.2
pdfplumber>=0.11
python-docx>=1.1
python-pptx>=1.0
openpyxl>=3.1
pillow>=10.0
pytesseract>=0.3.10
beautifulsoup4>=4.12
