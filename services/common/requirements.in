# Direct runtime dependencies of services/common (see pyproject.toml).
# Compiled to requirements.txt with `pip-compile --generate-hashes`; see
# issue #230. nexus-rag-common itself is installed from local source, not
# from this file.
opentelemetry-api>=1.30
opentelemetry-sdk>=1.30
opentelemetry-exporter-otlp-proto-http>=1.30
pyroscope-io>=1.2
pydantic>=2.7
PyJWT>=2.8
cryptography>=42.0
sqlmodel>=0.0.21
psycopg[binary]>=3.1
qdrant-client>=1.10
pymilvus>=2.5
fastembed>=0.6
boto3>=1.34
nats-py>=2.7
starlette>=0.36
